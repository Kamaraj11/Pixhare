# Pixhare — Production Requirements
# Python 3.10 or 3.11 recommended.
# DeepFace + TensorFlow have known issues with Python 3.12+ / 3.13.
#
# Install: pip install -r requirements.txt

# ── Web framework ────────────────────────────────────────────────────
flask==3.1.1
flask-sqlalchemy==3.1.1
flask-cors==5.0.1
werkzeug==3.1.3
PyJWT[crypto]>=2.10.1

# ── Database ──────────────────────────────────────────────────────────
sqlalchemy==2.0.41

# ── Environment / config ─────────────────────────────────────────────
python-dotenv==1.1.0

# ── Computer vision ───────────────────────────────────────────────────
# OpenCV (headless — no GUI dependency, suitable for servers)
opencv-python-headless==4.11.0.86

# DeepFace — unified facial analysis framework
# Internally uses RetinaFace (detector) + ArcFace (recognition model)
deepface==0.0.93

# TF-Keras required by DeepFace when using TensorFlow backend
tf-keras==2.19.0

# ── Vector search ─────────────────────────────────────────────────────
faiss-cpu==1.9.0

# ── Numerical computing ───────────────────────────────────────────────
numpy==1.26.4

# ── Image processing ─────────────────────────────────────────────────
pillow==11.1.0

# ── QR code generation ───────────────────────────────────────────────
qrcode[pil]==8.1

# ── LLM providers (all optional — system works without them) ─────────
groq==0.25.0

# ── Voice / TTS ───────────────────────────────────────────────────────
edge-tts==7.0.2

# ── Testing ───────────────────────────────────────────────────────────
pytest==8.3.5
pytest-flask==1.3.0

# ── Production server ─────────────────────────────────────────────────
gunicorn==23.0.0

# ── Optional: PostgreSQL driver (needed only if using Postgres) ───────
# psycopg2-binary==2.9.10
